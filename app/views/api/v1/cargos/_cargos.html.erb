<div class='lista-direita'>

  <table class="table table-striped table-hover">

    <thead class='head dark'>
      <tr>
        <th onclick="order('nome');">Nome</th>
        <th onclick="order('salario');">Salário</th>
      </tr>
    </thead>

    <tbody>
    <% cargos.each do |cargo| %>
    
      <tr class="<%= "active" if cargo.id.to_s == params[:id] %>" onclick="window.location.href='<%= edit_cargo_path(cargo) %>';">           
        <td><%= cargo.nome %></td>
        <td><%= cargo.salario %></td>
      </tr>
    <% end %>
    </tbody>
    
  </table>

</div>